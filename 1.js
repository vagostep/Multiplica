// Typescript
(function () {
    class Jugador {
    }
    ;
    function obtener11Ideal(tecnico, jugadores, fecha) {
        if (!tecnico || !jugadores || jugadores.length === 0 || !fecha) {
            throw new Error("No puede enviar parámetros vacíos");
        }
        console.debug(`No hay valores nulos`);
    }
    ;
    return "OK";
})();
